syntax = "proto3";

import "header.proto";

message JointState {
    Header header = 1;

    repeated string name = 2;
    repeated double position = 3;
    repeated double velocity = 4;
    repeated double effort = 5;
}